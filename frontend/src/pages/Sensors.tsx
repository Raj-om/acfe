import React from 'react';
import GlassCard from '../components/common/GlassCard';

const Sensors = () => {
  return (
    <div className="dashboard-grid">
      <GlassCard className="col-span-12" title="Sensor Management">
        <p>Detailed sensor configuration will go here.</p>
      </GlassCard>
    </div>
  );
};

export default Sensors;
